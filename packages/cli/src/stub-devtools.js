// Build-time stub for `react-devtools-core`, which ink imports statically in
// its DEV-mode helper. The CLI never runs with ink's DEV flag set, so this
// module is never used at runtime; the alias just keeps the bundle
// self-contained without shipping the devtools dependency.
export default undefined;
