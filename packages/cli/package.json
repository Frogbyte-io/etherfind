{
  "name": "etherfind",
  "version": "0.1.1",
  "description": "Find the IP address of a directly-connected Ethernet device. Run: npx etherfind",
  "license": "MIT",
  "type": "module",
  "bin": {
    "etherfind": "./dist/cli.js"
  },
  "files": ["dist"],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {
    "@etherfind/core": "*",
    "@types/react": "^19.0.0",
    "esbuild": "^0.28.2",
    "ink": "^6.0.0",
    "react": "^19.0.0"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "npm run typecheck && node build.mjs",
    "dev": "node --watch dist/cli.js"
  }
}
