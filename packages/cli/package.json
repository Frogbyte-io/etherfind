{
  "name": "etherfind",
  "version": "0.1.0",
  "description": "Find the IP address of a directly-connected Ethernet device. Run: npx etherfind",
  "license": "MIT",
  "type": "module",
  "bin": {
    "etherfind": "./dist/cli.js"
  },
  "files": ["dist", "!dist/**/*.test.*", "!dist/test-support/**", "!dist/**/*.tsbuildinfo"],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@etherfind/core": "^0.1.0",
    "ink": "^6.0.0",
    "react": "^19.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch dist/cli.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/frogbyte-io/etherfind.git",
    "directory": "packages/cli"
  }
}
